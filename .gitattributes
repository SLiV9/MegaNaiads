# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
*.txt text
*.md text
# Declare files that will always have a specific line endings on checkout.
*.gd eol=lf
*.godot eol=lf
*.tscn eol=lf
*.tres eol=lf
*.import eol=lf
# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.ase binary
*.ttf binary
*.tff filter=lfs diff=lfs merge=lfs -text
brains/*.pth.tar filter=lfs diff=lfs merge=lfs -text
bin/win64/*.dll filter=lfs diff=lfs merge=lfs -text
design/*.ase filter=lfs diff=lfs merge=lfs -text
Texturina.ttf filter=lfs diff=lfs merge=lfs -text
